apiVersion: v1
kind: Service
metadata:
  name: test-workflow
  namespace: namespace-workflow-1717493353059
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: test-workflow
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080